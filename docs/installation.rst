.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install targets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv targets
    $ pip install targets
